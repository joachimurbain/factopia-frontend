interface TrueFalseQuestion {
    name: string;
    title: string;
    question: string;
    solution: boolean;
    category: string[];
    image?: string;  // Optional image
  }

export default TrueFalseQuestion; 